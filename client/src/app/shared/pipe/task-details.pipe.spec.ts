import { TaskDetailsPipe } from './task-details.pipe';

describe('TaskDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
